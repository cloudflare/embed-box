include ../../../components/base-screenshot/mixin-annotation-arrow.pug

ol.steps
  li
    h2 Login to the <a target="_blank" href="https://www.shopify.com/login">Shopify store editor</a>.

  li
    h2 Navigate to <strong>Online Store <span class="with-more-icon-after"></span> Themes <span class="with-more-icon-after"></span> Edit HTML/CSS</strong>.
    p
      | In the left pane, click <strong>Online Store</strong>.
      | Click the <strong><code>[...]</code></strong> button in the header.

    p Then click <strong>Edit HTML/CSS</strong> in the menu that appears.

    figure
      img(src=require("./edit-template.jpg"))
      +annotation-arrow("nw")(style={top: "57%", left: "35.4%", width: "12%"} transform="rotate(60deg)")
      +annotation-arrow("ne")(style={top: "21%", left: "32.4%", width: "11%"} transform="rotate(41deg)")

  li
    p In the center column under <strong>Layout</strong>, Click <code>theme.liquid</code>.

    figure
      img(src=require("./choose-template.jpg"))
      +annotation-arrow("nw")(style={top: "48%", left: "35%", width: "16%"} transform="rotate(135deg)")

  li
    h2 Copy the code below.

    .copy-container(data-ref="copyContainers[]")
      button.button.primary.run(data-ref="copyButtons[]") Copy
      div.copyable(contenteditable)= this.copyText

    p Paste this code into your site’s &lt;#{this.location}&gt; tag.

    figure(data-ref="screenshotMounts[]" data-screenshot="installScript")

  li
    h2 Visit your site.

    p After saving the changes you made, visit your site in the browser.
    p You’re done!
