section(data-column data-component="site-type-search" data-event-receiver)
  header.header
    input.search(
      autofocus
      data-ref="search"
      placeholder=config.labels.searchPlaceholder
      tabindex="3"
      type="text")

  .types(data-column data-ref="typesContainer")

  footer.modal-footer
    button.primary.slim.more(data-action="next" data-ref="nextPageButton" tabindex="5")
      | #{config.labels.next}

