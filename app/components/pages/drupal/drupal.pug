- var count = 0

section
  h1 Installing #{config.appName} onto a Drupal site

  if config.beforeContent
    div(data-content-slot="before")
      != config.beforeContent

  if this.downloadURL
    - count++
    h2
      span.step-number.accent-background-color= count

      span
        a.more(target="_blank" href=this.downloadURL)= this.downloadLabel
        div= this.autoDownloadLabel

    p After downloading, don’t unzip the file.

  - count++
  h2
    span.step-number.accent-background-color= count
    span
      | Move the contents to <code>/sites/all/modules</code>

  figure
    img(src=require("./upload-plugin.png"))

  p Unzip the file and move it into the <strong>sites &gt; all &gt; modules</strong> folder inside your Drupal installation.

  p If you don’t have access to these files, please <a href="https://eager.io/contact">contact</a> us for help.

  - count++
  h2
    span.step-number.accent-background-color= count
    span Activate the plugin and view your site

  figure
    img(src=require("./activate-plugin.png"))

  p On the Modules page, scroll down to find the new “Eager” plugin.

  p Check the “Enabled” checkbox to activate the plugin, and click “Save configuration”.

  p After it activates, visit your site to see a welcome message letting you know the installation was successful!

  if config.afterContent
    div(data-content-slot="after")
      != config.afterContent
